# سجل التغييرات — Colab Desktop Runner

## [1.1.0] — 2026-02-27

### أضيف
- **نظام توفير الطاقة** بخمسة أوضاع: تلقائي/ذكي (الافتراضي)، متوازن، توفير قوي، توفير فائق، أداء مرتفع.
  - محرك سياسة مركزي قابل للاختبار (`PowerPolicyEngine`) — كل مستهلكي الطاقة يقرؤون منه.
  - الوضع الذكي يقرأ حالة الجهاز (بطارية/شحن/توفير طاقة النظام/شبكة محدودة) عبر طبقة Kotlin بلا أذونات إضافية.
  - فواصل إعادة الاتصال تتكيف مع الوضع (مضاعِف + Jitter عشوائي لمنع التزامن).
  - وضع "توفير فائق" يعطّل إبقاء الشاشة مضاءة ويقلّل المحاولات.
  - إيقاف كامل لأي عمل دوري في الخلفية (احترام Doze/App Standby).
  - مؤشر حي للوضع الفعلي في شاشة الإعدادات.
- **نظام التحديث الآمن داخل التطبيق** عبر GitHub Releases:
  - بيان تحديث `latest.json` موقّع بتوقيع Ed25519 (المفتاح العام مثبّت داخل التطبيق).
  - حماية من Replay (رقم تسلسلي متزايد)، وDowngrade، وانتهاء الصلاحية، واستبدال الحزمة.
  - **تحديثات تفاضلية (Delta / bsdiff)**: تنزيل رقعة صغيرة بدل التطبيق كاملًا،
    مع قراءة APK المثبّت والتحقق من بصمته، وتطبيق الرقعة في Isolate،
    والتحقق النهائي SHA-256 بايت-ببايت قبل التثبيت.
  - استئناف التنزيل (HTTP Range) والتراجع التلقائي لمسار APK الكامل عند أي فشل.
  - خيار "Wi-Fi فقط" (مفعّل افتراضيًا) وعرض حجم البيانات الموفَّرة.
  - التثبيت النهائي عبر نافذة النظام الرسمية (PackageInstaller) بموافقة المستخدم.
  - خط إنتاج إصدارات (`tools/release_pipeline.py`) يولّد الرقع من الإصدارات
    السابقة **ويتحقق من إعادة البناء بايت-ببايت قبل النشر** (شرط قبول إلزامي).
- **CI عبر GitHub Actions**: تحليل + اختبارات + بناء عند كل Push/PR (بلا أي أسرار في الملفات).

### تحسّن
- حماية من النقر المتكرر على أزرار اختيار الملفات.
- 62 اختبارًا آليًا (كانت 22) تغطي سياسة الطاقة، تطبيق رقع bsdiff الحقيقية،
  وحمايات المانيفست (Replay/Downgrade/انتهاء الصلاحية).

### الأمان
- `.gitignore` مشدَّد: مفاتيح التوقيع (`*.jks`, `key.properties`) والمفاتيح
  الخاصة محظورة نهائيًا من Git.
- إذن `REQUEST_INSTALL_PACKAGES` أُضيف حصريًا لتثبيت التحديثات عبر نافذة
  النظام الرسمية — لا يُستخدم لأي غرض آخر.

## [1.0.0] — الإصدار الأول

- فتح Google Colab عبر Custom Tabs الرسمية (تسجيل دخول Google آمن).
- اختيار سكربت Python عبر Storage Access Framework (بدون أذونات تخزين).
- نسخ كود التشغيل الجاهز إلى الحافظة.
- مساعد تشغيل من 8 خطوات بتحكم المستخدم الكامل.
- متصفح سطح مكتب مخصص: HTTPS فقط، موافقة على النطاق، وضع السحب الدقيق
  بثلاث حساسيات، إعادة اتصال بتدرج 2/5/10/20 ثانية، إبقاء الشاشة مضاءة،
  رفع ملفات عبر نافذة النظام.
- شاشة خصوصية عند أول تشغيل، بلا تحليلات وبلا إعلانات وبلا جمع بيانات.
