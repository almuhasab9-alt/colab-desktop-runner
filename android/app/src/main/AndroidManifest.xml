<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.colabdesktoprunner.runner">

    <!--
      الأذونات وسبب استخدامها:
      - INTERNET: الوصول إلى Google Colab ورابط سطح المكتب (HTTPS فقط).
      لا يوجد أي إذن تخزين شامل (MANAGE_EXTERNAL_STORAGE) — يتم اختيار
      الملفات عبر Storage Access Framework فقط دون أذونات إضافية.
      لا يوجد QUERY_ALL_PACKAGES ولا AccessibilityService.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- طلب تثبيت التحديثات عبر نافذة النظام الرسمية فقط (بموافقة المستخدم) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="Colab Desktop Runner"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FileProvider لمشاركة ملف APK المحدّث مع مُثبّت النظام فقط -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.colabdesktoprunner.runner.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>
    </application>

    <queries>
        <!-- مطلوب لفتح Custom Tabs والمتصفح الافتراضي (تسجيل دخول Google الرسمي) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>

